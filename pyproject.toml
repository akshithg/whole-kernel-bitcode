[project]
name = "whole-kernel-bitcode"
version = "0.2.0"
description = "Generate whole-kernel LLVM bitcode from a kernel build tree"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
kbitcode = "kbitcode:main"
